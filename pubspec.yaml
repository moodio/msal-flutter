name: msal_flutter
description: A Microsoft Authentication Library for Android and iOS
version: 3.0.0-nullsafety.0
homepage: https://github.com/moodio/msal-flutter
repository: https://github.com/moodio/msal-flutter
environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  plugin:
    androidPackage: uk.co.moodio.msal_flutter
    pluginClass: MsalFlutterPlugin